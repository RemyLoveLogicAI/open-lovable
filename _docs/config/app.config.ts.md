<!-- METADATA: {"source_path": "config/app.config.ts", "source_sha": "", "extraction_quality": "raw_source", "model": "gpt-5-mini", "generated_at": "2026-07-09T07:49:57Z", "doc_type": "file"} -->
<details>
<summary>Documentation Metadata (click to expand)</summary>

```json
{
  "doc_type": "file_overview",
  "file_path": "config/app.config.ts",
  "source_hash": "f4fb33cd9eb6738976234bb40cc3f2e790e5727e944c19264ac016748c776b63",
  "last_updated": "2026-07-09T07:49:51.805046+00:00",
  "tokens_used": 1916,
  "complexity_score": 1,
  "estimated_review_time_minutes": 8,
  "external_dependencies": []
}
```

</details>

[Documentation Home](../README.md) > [config](./README.md) > **app.config**

---

# app.config.ts

> **File:** `config/app.config.ts`

![Complexity: Low](https://img.shields.io/badge/Complexity-Low-green) ![Review Time: 8min](https://img.shields.io/badge/Review_Time-8min-blue)

## 📑 Table of Contents


- [Overview](#overview)
- [Dependencies](#dependencies)
- [Architecture Notes](#architecture-notes)
- [Maintenance Notes](#maintenance-notes)
- [Functions and Classes](#functions-and-classes)

---

## Overview

This TypeScript file defines a centralized application configuration object (appConfig) that collects various configurable settings for different areas of the application such as sandbox/E2B, AI model options, code application behavior, UI, development settings, package installation, file management, and API endpoints. It exports the appConfig constant as the default export and provides two helper functions: a type-safe getConfig generic to retrieve top-level configuration sections with proper TypeScript typing, and getConfigValue which accepts a dot-separated path string to resolve nested config values at runtime.

The configuration includes concrete numeric and boolean defaults (e.g., timeouts, delays, toggles), lists (e.g., available AI models, file exclude patterns), and a computed getter (timeoutMs) that derives milliseconds from a minutes setting. The file also uses a small type cast (as Record<string, string>) for model display names to help with typing in the ai section.

## Dependencies

No dependencies identified.

## 📁 Directory

This file is part of the **config** directory. View the [directory index](_docs/config/README.md) to see all files in this module.

## Architecture Notes

- Exports a single centralized configuration object (appConfig) that organizes settings by feature/area.
- Includes both statically defined values and a computed getter (timeoutMs) derived from another setting.
- Provides a generic, type-safe accessor (getConfig<K>) for top-level config sections and a dynamic string-path resolver (getConfigValue) for nested access.
- Uses a type assertion (as Record<string, string>) to type the model display name map.
- Note: the provided summary is based on a raw source extraction with limited structural metadata, so descriptions are confined to the exported constants and functions present in the extracted file.
- Documentation generated from raw source — no structural extractor was available for this language; descriptions may be less reliable.

## Maintenance Notes

- Declare and export a centralized appConfig object containing grouped configuration for e2b sandbox, AI models, code application behavior, UI, development, package installation, file handling, and API settings.
- Provide a type-safe getter getConfig<K> that returns a top-level configuration section with preserved TypeScript types.
- Provide a runtime helper getConfigValue(path: string) to access nested configuration values using dot-separated paths.
- Export appConfig as the default export so other modules can import the full configuration object.

---

## Navigation

**↑ Parent Directory:** [Go up](_docs/config/README.md)

---

*This documentation was automatically generated by AI ([Woden DocBot](https://github.com/marketplace/ai-document-creator)) and may contain errors. It is the responsibility of the user to validate the accuracy and completeness of this documentation.*
